# AI image generation
replicate>=1.0.0

# LLM prompt refinement (Gemini)
google-generativeai>=0.8.0

# Desktop UI framework
nicegui>=2.0.0

# Image processing
Pillow>=10.0.0
rembg>=2.0.0          # Background removal
onnxruntime>=1.15.0   # Required by rembg

# Configuration
pyyaml>=6.0

# HTTP (for API status checks)
httpx>=0.27.0

# Build / packaging
pyinstaller>=6.0.0    # Bundle into standalone app

# Development
pytest>=8.0.0
pytest-asyncio>=0.24.0
